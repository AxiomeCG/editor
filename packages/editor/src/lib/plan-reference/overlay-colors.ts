/**
 * Plan-workspace overlay colours, shared by the 2D and 3D layers. Candidates
 * stay the tool's violet; selection switches hue (amber, like the anchor
 * handle) because an opacity change alone disappears on blue/violet plan ink.
 */
export const PLAN_CANDIDATE_COLOR = '#8b5cf6'
export const PLAN_SELECTED_COLOR = '#f59e0b'
